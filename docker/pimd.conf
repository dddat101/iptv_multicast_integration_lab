# Minimal placeholder. pimd syntax differs by distribution/version.
# Review `pimd --version` and `man pimd` before enabling ENABLE_WAN_QUERIER=1.
# The project intentionally does not start pimd until a validated config is supplied.
